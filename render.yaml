services:
  - type: web
    name: accommodation-photo-landing
    env: static
    rootDir: .
    buildCommand: npm ci && npm run build
    staticPublishPath: dist/accommodation-photo-landing/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
