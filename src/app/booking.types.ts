export type PackageOffer = {
  nameKey: string;
  price: string;
  deliverableKey: string;
  noteKey: string;
  googlePacketValue: 'Packet1' | 'Packet2' | 'Packet3';
  image: {
    src: string;
    altKey: string;
  };
};
